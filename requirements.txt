# Core deep-learning stack
# Any PyTorch build with CUDA kernels matching your GPU works (CUDA 12.x and
# 13.x both verified, e.g. torch 2.8.0+cu128 on RTX 4090 / sm_89 and
# torch 2.11.0+cu130 on RTX 5090 / sm_120). If your GPU is newer than the
# default PyPI wheels, point pip at the matching CUDA wheel index:
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
torch>=2.4

numpy>=1.24
pandas>=2.0
pyyaml>=6.0
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.65

# OPTIONAL: official Mamba CUDA kernels (fast path). The project ships a
# pure-PyTorch Mamba fallback, so these are NOT required to train. If your
# toolchain can build them, install for a speed-up:
#   pip install causal-conv1d>=1.4.0
#   pip install mamba-ssm>=2.2.0

# OPTIONAL: xlsx table generation for the cumulative-ablation suite
# (scripts/ablation/update_cumulative_table.py). Markdown/JSON outputs do
# not need it.
#   pip install openpyxl>=3.1
# MambaLayer auto-detects and uses them when present; otherwise it runs pure-torch.
